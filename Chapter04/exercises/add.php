<?php
/*
 * Define a function which will take two arguments,
 * adds both parameters and returns the result.
 *
 * Print the result: "The sum of  $arg1 and $arg2 is: $result".
 *
 * Name your function add, so that the unit test will pick it up.
 *
 * To run the AddTest, open a terminal and go to Chapter04/tests.
 * At the prompt type: phpunit AddTest.php [Enter]. The test should now run and pass.
 */
