<?php
declare(strict_types=1);
/*
 * Create a variable anonymous function which will print its single parameter while prepending “Hello ".
 * So if you call it with the argument "John" it will print "Hello John".
 *
 * Name your function greeting, so that the unit test will pick it up.
 *
 * To run the VariableHelloTest, open a terminal and go to Chapter04/tests.
 * At the prompt type: phpunit VariableHelloTest.php [Enter]. The test should now run and pass.
 */
